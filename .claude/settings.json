{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; echo \"$f\" | grep -qE '\\.(ts|tsx)$' && npm run lint -- --quiet 2>&1 | head -50; } 2>/dev/null || true",
            "statusMessage": "Linting...",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
